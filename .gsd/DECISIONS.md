# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Execution path authority | Script-first canonical path; notebook training disabled | “Tek gerçeklik” hedefi için iki eğitim yolu bırakmamak | No |
| D002 | M001 | data | Evaluation split | Train<=2022, Val=2023, Test=2024-2025 | Val/test illüzyonunu engellemek için sabit walk-forward standardı | No |
| D003 | M001 | pattern | Men/Women model handling | Separate training/eval artifacts for each | Cinsiyet bazlı davranış ayrışmasını görünür ve yönetilebilir tutmak | No |
| D004 | M001 | convention | Reproducibility gate tolerance | Same commit+seed requires |ΔBrier| <= 1e-4 | Run tekrar üretilebilirliğini ölçülebilir kontrata bağlamak | Yes — if numerical backend behavior changes |
| D005 | M001 | api | Regression gate policy | Brier mandatory, calibration degradation = fail, AUC informational | Tek metrik körlüğünü engelleyip kalite düşüşünü otomatik yakalamak | Yes — threshold tuning after baseline history |
| D006 | M001 | scope | External systems boundary | Local + git + Kaggle only (no MLflow/W&B/DB/scheduler now) | Önce canonical hattı stabilize etmek, erken MLOps entegrasyonundan kaçınmak | Yes — after M001 stabilization |
| D007 | M001/S01 | contract | Canonical run artifact layout | Each run writes `mania_pipeline/artifacts/runs/<run_id>/run_metadata.json` + `stage_events.jsonl` | S02+ gate and S06 reproducibility checks need a stable machine-readable run surface | Yes — if contract versioning is needed later |
| D008 | M001/S01/T01 | test-contract | Orchestrator test seam | `run_pipeline.py` must expose `build_run_context(seed=...)`, `main(argv=None)`, `CANONICAL_STAGES`, and `STAGE_HANDLERS` for contract-driven monkeypatch tests | T01 locks CLI lifecycle behavior before implementation; this seam keeps tests deterministic without running real feature/train workloads | Yes — if we later introduce explicit interface/versioned adapter |
| D009 | M001/S01/T02 | runtime-wiring | Canonical orchestrator stage integration | `run_pipeline.py` dynamically loads `02_feature_engineering.py` and `03_lgbm_train.py` via importlib and rebinds their data/artifact dirs to absolute repo paths before calling Python-level functions | Numeric-prefixed script names are not import-safe as regular modules; dynamic loading + absolute path rebinding keeps a single canonical command deterministic across caller cwd contexts | Yes — if scripts are later packaged as importable modules |
| D010 | M001/S02 | gate-wiring | Split/leakage gates run inside existing `feature` stage (no new stage) and persist under `stage_outputs.feature.gates` | Keep canonical stage order contract stable while enforcing R002/R004 fail-fast before train; reuse existing stage failure/event surfaces | Yes — if future contract versioning requires dedicated gate stage |
| D011 | M001/S02/T01 | gate-contract | Leakage validator uses exact forbidden column set + explicit namespace allowlist (no keyword heuristics) | Avoid false positives while still hard-failing raw/post-game leakage columns with machine-readable `blocking_rule` evidence | Yes — expand allowlist/forbidden set if feature schema intentionally changes |
| D012 | M001/S02/T02 | gate-payload | `stage_outputs.feature.gates.{men,women}` stores a canonical aggregate gate payload (`pass`, `blocking_rule`, `reason`, `evidence`) with nested `evidence.split` + `evidence.leakage` sub-results | Keeps persisted gate diagnostics machine-readable without changing canonical stage order or adding extra artifact files | Yes — if we later version gate contracts or split them into a dedicated stage |
| D013 | M001/S03/T01 | train-contract | `train_baseline` returns `(model, payload)` where payload includes `gender`, `metrics_by_split` (`Train/Val/Test` × `brier/logloss/auc` + `auc_reason`), `feature_snapshot`, and `best_iteration` | R005/R006 needs one Men/Women-shared training core with machine-readable split metrics; single-class AUC must surface deterministic reason instead of silent failure | Yes — if we version the train payload schema in later milestones |
| D014 | M001/S03/T02 | orchestrator-contract | `run_pipeline.py::stage_train` must hard-require `stage_outputs.feature.gates.{men,women}.pass==True`, then persist per-gender train payload under `stage_outputs.train` (`genders`, `metrics_by_split`, `feature_snapshot`, `best_iteration`, `models`); `stage_eval_report` must publish `metrics_table` + `side_by_side_summary` from that contract | S03 demo and R006 need canonical runtime artifacts (not notebook/stateful logs) to expose Men/Women split metrics and explicit test-side comparison while preventing gate-bypass training | Yes — if run artifact schema is versioned in later milestones |
| D015 | M001/S03/T03 | execution-authority-guard | `03_model_training.ipynb` is analysis/reporting-only and a pytest guard must fail if notebook code cells contain training/persistence primitives (`LGBM*`, `.fit(`, `joblib.dump`, `pickle.dump`, `lgb.train`) | R003/R019 requires technical enforcement against notebook authority drift; policy must be machine-checked in CI, not convention-only | Yes — forbidden pattern set can evolve with modeling stack changes |
| D016 | M001/S04 | reporting-contract | Calibration artifact generation seam | Compute calibration inside `stage_eval_report` by reloading per-gender models and rescoring canonical splits; persist outputs as `calibration_bins.csv` + `calibration_report.json` (no per-row arrays in run metadata) | S03 stage topology and contracts stay stable while R007 gains machine-readable artifacts without metadata bloat | Yes — if a dedicated calibration stage or calibrator-fit flow is introduced later |
| D017 | M001/S04 | metric-contract | High-prob overconfidence band definition | Use fixed high-prob band `p>=0.8` with mandatory `sample_count` and explicit empty-band reason fields | `p>=0.9` produced sparse/empty women bins in probe runs; `0.8` gives usable drift signal for S06 gate inputs | Yes — threshold can be re-tuned with larger run history |
| D018 | M001/S04/T01 | failure-contract | Calibration failure handling semantics | Fail fast on model/feature-column contract violations; emit structured `reason` diagnostics for non-fatal sparse states (`split_empty`, `empty_high_prob_band`) | Hard contract breaks should stop the stage, but sparse data states are expected and should remain machine-inspectable for downstream gates | Yes — if future policy treats sparse splits as hard failures |
| D019 | M001/S04/T02 | test-contract | CLI stage-topology lock strategy | `test_run_pipeline_cli.py` must assert module-declared `CANONICAL_STAGES == ('feature','train','eval_report','artifact')` before monkeypatching handlers, and treat calibration as `stage_outputs.eval_report.calibration` (not a stage) | Prevents monkeypatched tests from masking accidental stage-list expansion while keeping calibration wiring backward-compatible with existing lifecycle contract | Yes — if orchestration contract is versioned with an explicit stage graph adapter |
| D020 | M001/S05 | reporting-contract | Governance + ablation stage seam | Implement R008/R009 inside existing `stage_eval_report` and persist as `governance_ledger.csv` + `ablation_report.json` with metadata wiring; do not add a new pipeline stage | Preserves canonical stage-order contract while exposing machine-readable governance surfaces for S06 regression gating | Yes — if future contract versioning introduces a dedicated governance stage |
| D021 | M001/S05 | runtime-policy | Controlled ablation execution policy | Use deterministic, capped suspicious-group subset retrain (`train_baseline` with run seed) and report per-gender Val/Test deltas for Brier/LogLoss/AUC/Calibration, with explicit skip reasons | Avoids single-metric false decisions and runtime blow-up while keeping ablation evidence reproducible and auditable | Yes — cap/selection thresholds can be tuned after historical run data accumulates |
| D022 | M001/S05/T01 | governance-policy | Ledger default action contract | Derive `default_action` deterministically from per-feature model importance (`<=0 => drop`, bottom-rank tail => `candidate`, else `keep`) and always exclude `MEN_ONLY_FEATURES` from women rows | Locks R008 to a reproducible, machine-readable policy without forcing invalid women evidence rows for men-only namespaces | Yes — thresholds/group heuristics can be tuned after ablation history accumulates |
