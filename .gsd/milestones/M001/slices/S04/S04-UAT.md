# S04: Calibration Layer + Overconfidence/Drift Reporting — UAT

**Milestone:** M001
**Written:** 2026-03-14

## UAT Type

- UAT mode: mixed (artifact-driven + live-runtime)
- Why this mode is sufficient: S04 output is primarily machine-readable artifacts/contracts generated by canonical runtime; proof requires both actual run execution and artifact schema/diagnostic verification.

## Preconditions

- Repo root: `C:\Users\Gaming\Desktop\projects\yarismatahmin`
- Python venv available at `./venv/Scripts/python`
- Kaggle source CSV files present under expected `mania_pipeline` data paths
- No changes pending that break S03 canonical stage contract (`feature -> train -> eval_report -> artifact`)

## Smoke Test

1. Run:
   - `./venv/Scripts/python mania_pipeline/scripts/run_pipeline.py --seed 42 --run-label s04_uat_smoke`
2. Identify latest run folder under `mania_pipeline/artifacts/runs/`.
3. **Expected:** Latest run contains `calibration_bins.csv`, `calibration_report.json`, `eval_report.json`, `run_metadata.json`, and command exits with succeeded run status.

## Test Cases

### 1. Calibration artifact contract (schema + gender/split coverage)

1. Execute:
   - `./venv/Scripts/python -m pytest mania_pipeline/tests/test_run_pipeline_s04_calibration_contract.py`
2. Execute artifact assertion:
   - `./venv/Scripts/python -c "import json, pathlib, pandas as pd; runs=pathlib.Path('mania_pipeline/artifacts/runs'); run=max(runs.iterdir(), key=lambda p:p.stat().st_mtime); rp=run/'calibration_report.json'; bp=run/'calibration_bins.csv'; ep=run/'eval_report.json'; assert rp.exists() and bp.exists() and ep.exists(); report=json.loads(rp.read_text(encoding='utf-8')); eval_report=json.loads(ep.read_text(encoding='utf-8')); bins=pd.read_csv(bp); assert {'gender','split','bin_left','bin_right','sample_count','pred_mean','actual_rate','gap'}.issubset(bins.columns); assert set(report['calibration_summary'].keys())=={'men','women'}; assert set(bins['split'])=={'Train','Val','Test'}; assert 'calibration' in eval_report; print('contract-ok', run.name)"`
3. **Expected:** Pytest passes; assertion prints `contract-ok <run_id>` and no schema/gender/split assertion fails.

### 2. Backward compatibility with S03 eval + CLI lifecycle contract

1. Execute:
   - `./venv/Scripts/python -m pytest mania_pipeline/tests/test_run_pipeline_s03_eval_contract.py mania_pipeline/tests/test_run_pipeline_cli.py`
2. **Expected:** All tests pass; no regression in canonical stage topology and no requirement for a new calibration stage.

### 3. Observability wiring and stage order continuity

1. Execute:
   - `./venv/Scripts/python -c "import json, pathlib, pandas as pd; run=max(pathlib.Path('mania_pipeline/artifacts/runs').iterdir(), key=lambda p:p.stat().st_mtime); cal=json.loads((run/'calibration_report.json').read_text(encoding='utf-8')); evalr=json.loads((run/'eval_report.json').read_text(encoding='utf-8')); meta=json.loads((run/'run_metadata.json').read_text(encoding='utf-8')); bins=pd.read_csv(run/'calibration_bins.csv'); events=[json.loads(l) for l in (run/'stage_events.jsonl').read_text(encoding='utf-8').splitlines() if l.strip()]; stage_starts=[e['stage'] for e in events if e.get('status')=='started']; assert stage_starts==['feature','train','eval_report','artifact']; assert {'bins_csv','report_json','calibration_summary'}.issubset(evalr['calibration']); assert 'calibration' in meta['stage_outputs']['eval_report']; assert set(cal['calibration_summary'])=={'men','women'}; assert set(bins['gender'])=={'men','women'}; print('observability-ok', run.name)"`
2. **Expected:** Prints `observability-ok <run_id>`; calibration payload present in both eval report and run metadata; stage order remains unchanged.

## Edge Cases

### Empty high-probability band (`p>=0.8`) diagnostic path

1. Execute:
   - `./venv/Scripts/python -m pytest mania_pipeline/tests/test_run_pipeline_s04_calibration_contract.py -k "empty_high_prob"`
2. **Expected:** Test passes and verifies non-fatal diagnostic reason (`empty_high_prob_band`) is emitted in high-prob summary when band is empty.

### Feature-column mismatch hard-fail path

1. Execute:
   - `./venv/Scripts/python -m pytest mania_pipeline/tests/test_run_pipeline_s04_calibration_contract.py -k "feature_column_mismatch"`
2. **Expected:** Test passes by confirming runtime raises explicit mismatch failure (fail-fast contract violation), not silent fallback.

## Failure Signals

- Missing `calibration_bins.csv` or `calibration_report.json` in latest run folder.
- `eval_report.json` missing top-level `calibration` block.
- `run_metadata.json.stage_outputs.eval_report` missing `calibration` payload.
- `calibration_bins.csv` missing any required columns: `gender, split, bin_left, bin_right, sample_count, pred_mean, actual_rate, gap`.
- Stage lifecycle order drift in `stage_events.jsonl` away from `feature -> train -> eval_report -> artifact`.
- High-prob/sparse-state diagnostics absent (`reason` not present where expected).

## Requirements Proved By This UAT

- R007 — Calibration bins + calibration summary metrics + overconfidence/drift diagnostics are generated and wired into canonical run artifacts.

## Not Proven By This UAT

- R008/R009 — Feature governance and ablation reporting (S05 scope).
- R010/R011/R018 full gate behavior — artifact contract hardening, reproducibility tolerance, and regression pass/fail policy (S06 scope).
- R012 — Submission generation/validation path (S07 scope).

## Notes for Tester

- Women data pipeline logs expected warnings for unavailable raw files (`WNCAATourneySeedRoundSlots.csv`, `WTeamCoaches.csv`); these warnings are not S04 failures.
- Use the latest run directory by modified time to avoid validating stale artifacts.
- If assertions fail, inspect in this order: `run_metadata.json` → `eval_report.json` → `calibration_report.json` → `stage_events.jsonl`.
